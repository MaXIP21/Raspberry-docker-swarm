version: '3.7'

services:
  
  consul-server:
    image: hashicorp/consul:1.10.0
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.hostname == ${NODENAME}
    restart: always
    volumes:
     - consul_nfs/config/server.json:/consul/config/server.json
    networks:
      - bridge
    ports:
      - "8500:8500"
      - "8600:8600/tcp"
      - "8600:8600/udp"
    command: "agent"

  consul-client:
    image: hashicorp/consul:1.10.0
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.hostname == ${CLIENT_NODENAME}
    restart: always
    volumes:
     - consul_nfs/config/client.json:/consul/config/client.json
    networks:
      - bridge
    command: "agent"

volumes:
  consul_nfs:
    driver_opts:
      type: "nfs"
      o: "addr=${NFS_HOST},nfsvers=4"
      device: ":/docker/consul"
      
networks:
  bridge:
